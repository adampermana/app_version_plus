/// Enum representing the type of device/platform
enum DeviceType {
  /// Android device (non-Huawei)
  android,

  /// iOS/iPadOS device
  ios,

  /// Huawei device (using Huawei AppGallery)
  huawei,

  /// Unknown or unsupported platform
  unknown,
}
